# Copyright 2026 fitctl contributors
# SPDX-License-Identifier: Apache-2.0

[build-system]
requires = ["maturin==1.12.6"]
build-backend = "ci.build_backend"
backend-path = ["."]

[project]
name = "fitctl"
version = "0.1.1"
description = "Python bindings for fitctl"
readme = "README.md"
requires-python = ">=3.12"
dynamic = ["license"]
license-files = ["LICENSE", "NOTICE", "THIRD_PARTY_LICENSES.txt"]
dependencies = []

[project.urls]
Repository = "https://github.com/tznurmin/fitctl-python"
Documentation = "https://github.com/tznurmin/fitctl-python/tree/main/docs"
Issues = "https://github.com/tznurmin/fitctl-python/issues"

[tool.maturin]
manifest-path = "rust/Cargo.toml"
python-source = "python"
module-name = "fitctl._native"
sdist-generator = "git"
features = ["extension-module"]

[tool.maturin.sbom]
rust = false
